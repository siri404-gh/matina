/**
 *
 * @param {string} theme
 * @return {Object}
 */
export default theme => ({
  app: {
    maxWidth: 1000,
    margin: 'auto',
    // flexGrow: 1,
    minHeight: 440,
    zIndex: 1,
    overflow: 'hidden',
    position: 'relative',
    // display: 'flex',
    width: '100%',
  },
});
